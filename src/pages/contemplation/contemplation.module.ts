import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ContemplationPage } from './contemplation';

@NgModule({
  declarations: [
    ContemplationPage,
  ],
  imports: [
    IonicPageModule.forChild(ContemplationPage),
  ],
})
export class ContemplationPageModule {}
